# URL of the Evergreen agent API — must be reachable from the browser
# In local dev: http://localhost:8000
# In Docker: keep as http://localhost:8000 (agent port is exposed on the host)
NEXT_PUBLIC_API_URL=http://localhost:8000
