services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: evergreen-postgres
    environment:
      POSTGRES_USER: evergreen
      POSTGRES_PASSWORD: evergreen
      POSTGRES_DB: evergreen
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evergreen -d evergreen"]
      interval: 5s
      timeout: 5s
      retries: 5

  pipeline:
    build:
      context: .
      dockerfile: Dockerfile.pipeline
    container_name: evergreen-pipeline
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://evergreen:evergreen@postgres:5432/evergreen
      # Cron schedule (default: every Sunday 02:00)
      PIPELINE_CRON: ${PIPELINE_CRON:-0 2 * * 0}
      RUN_ON_STARTUP: ${RUN_ON_STARTUP:-true}
      # Fixed container path; bind-mount the actual key below
      GOOGLE_SA_KEY_PATH: /run/secrets/sa_key.json
    volumes:
      # Mount SA key read-only if the host path is set; falls back to /dev/null if unset
      - ${GOOGLE_SA_KEY_PATH:-/dev/null}:/run/secrets/sa_key.json:ro
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: evergreen-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://evergreen:evergreen@postgres:5432/evergreen
      PUBLIC_HOST: ${PUBLIC_HOST:-localhost}
      AGENT_PORT: "8000"
      GOOGLE_OAUTH_TOKEN_PATH: ${GOOGLE_OAUTH_TOKEN_PATH:-}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        # Must be reachable from the user's browser — keep as localhost when running locally
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
    container_name: evergreen-frontend
    ports:
      - "3000:3000"
    depends_on:
      agent:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
